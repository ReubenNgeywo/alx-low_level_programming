#include "main.h"

/**
 * add - adds two integers and returns the result
 * @r: int to be added to n
 * @n: int to be added to r
 * Return: sum of r and n
 */
int add(int r, int n)
{
	int c;

	c = r + n;

	return (c);
}
